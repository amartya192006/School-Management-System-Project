# School-Management-System-Project
This project demonstrates Python–MySQL integration where user input is taken through Python and stored in a MySQL database. It focuses on basic database connectivity, backend data storage, and execution of SQL queries using Python.
